import React from "react"

const ProjectCard = () => {
  return (
    <div>
      <h2 style={{ fontSize: `30px` }}>Latest Project:</h2>
      <strong style={{ fontSize: `26px` }}>Project Name</strong>
      <p style={{ fontSize: `24px`, fontWeight: `300` }}>
        Brief project description.
      </p>
    </div>
  )
}

export default ProjectCard
