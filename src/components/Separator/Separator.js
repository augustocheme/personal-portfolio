import React from "react"

const Separator = () => {
  return (
    <div
      style={{
        display: `block`,
        height: `8px`,
        width: `435px`,
        background: `#000`,
        margin: `40px 0`,
      }}
    />
  )
}

export default Separator
